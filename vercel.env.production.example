# Vercel Production environment variables
# Copy values from Supabase Dashboard + Stripe Dashboard (LIVE mode).
# Vercel → Project → Settings → Environment Variables → Production

VITE_SUPABASE_URL=https://kong-production-3917.up.railway.app
VITE_SUPABASE_ANON_KEY=
VITE_STRIPE_PUBLISHABLE_KEY=pk_live_

# Stripe live webhook (separate from test):
# Create endpoint → https://<supabase>/functions/v1/stripe-webhook
# Set STRIPE_WEBHOOK_SECRET in Supabase secrets (not Vercel)

# Supabase secrets stay in Supabase only — see GROWTH-SETUP.md
